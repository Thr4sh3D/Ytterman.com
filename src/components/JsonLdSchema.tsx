/**
 * @deprecated Static structured data is emitted once by RouteSeo.
 * Kept temporarily so existing page imports do not create a second schema layer.
 */
export const JsonLdSchema = (_props: { type?: 'ProfessionalService' | 'LocalBusiness' }) => null;

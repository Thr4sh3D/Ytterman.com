import KontrollansvarigCityPage from './KontrollansvarigCityPage';
import { cityData } from '@/content/kontrollansvarigCityData';

const KontrollansvarigSolleftea = () => (
  <KontrollansvarigCityPage cityData={cityData.solleftea} />
);

export default KontrollansvarigSolleftea;

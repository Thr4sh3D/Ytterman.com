import KontrollansvarigCityPage from './KontrollansvarigCityPage';
import { cityData } from '@/content/kontrollansvarigCityData';

const KontrollansvarigKramfors = () => (
  <KontrollansvarigCityPage cityData={cityData.kramfors} />
);

export default KontrollansvarigKramfors;
